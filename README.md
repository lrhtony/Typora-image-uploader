# Typora Image Uploader
A simple image uploader plugin for Typora that uploads images to my image hosting service.

## Usage
```bash
python "main.py" -f "${filepath}" 
```

> [!IMPORTANT]  
> There must be a space after `${filepath}`.


